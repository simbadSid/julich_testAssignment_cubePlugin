    libs.clear();
    ldflags.clear();
    rpaths.clear();
    dependency_las.clear();
    libs.push_back( "-lz" );
    if ( la_objects->find( "libcube4" ) == la_objects->end() )
    {
        (*la_objects)[ "libcube4" ] =
            la_object( "libcube4",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-4.4-TP3/build-frontend",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-install/lib",
                       libs,
                       ldflags,
                       rpaths,
                       dependency_las );
    }

    libs.clear();
    ldflags.clear();
    rpaths.clear();
    dependency_las.clear();
    libs.push_back( "-lz" );
    dependency_las.push_back( "libcube4" );
    if ( la_objects->find( "libcube.tools.common" ) == la_objects->end() )
    {
        (*la_objects)[ "libcube.tools.common" ] =
            la_object( "libcube.tools.common",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-4.4-TP3/build-frontend",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-install/lib",
                       libs,
                       ldflags,
                       rpaths,
                       dependency_las );
    }

    libs.clear();
    ldflags.clear();
    rpaths.clear();
    dependency_las.clear();
    libs.push_back( "-lz" );
    if ( la_objects->find( "libcube4" ) == la_objects->end() )
    {
        (*la_objects)[ "libcube4" ] =
            la_object( "libcube4",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-4.4-TP3/build-frontend",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-install/lib",
                       libs,
                       ldflags,
                       rpaths,
                       dependency_las );
    }

    libs.clear();
    ldflags.clear();
    rpaths.clear();
    dependency_las.clear();
    libs.push_back( "-lz" );
    dependency_las.push_back( "libcube.tools.common" );
    dependency_las.push_back( "libcube4" );
    if ( la_objects->find( "libcube.tools.library" ) == la_objects->end() )
    {
        (*la_objects)[ "libcube.tools.library" ] =
            la_object( "libcube.tools.library",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-4.4-TP3/build-frontend",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-install/lib",
                       libs,
                       ldflags,
                       rpaths,
                       dependency_las );
    }

    libs.clear();
    ldflags.clear();
    rpaths.clear();
    dependency_las.clear();
    libs.push_back( "-lz" );
    dependency_las.push_back( "libcube4" );
    if ( la_objects->find( "libcube.tools.common" ) == la_objects->end() )
    {
        (*la_objects)[ "libcube.tools.common" ] =
            la_object( "libcube.tools.common",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-4.4-TP3/build-frontend",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-install/lib",
                       libs,
                       ldflags,
                       rpaths,
                       dependency_las );
    }

    libs.clear();
    ldflags.clear();
    rpaths.clear();
    dependency_las.clear();
    libs.push_back( "-lz" );
    if ( la_objects->find( "libcube4" ) == la_objects->end() )
    {
        (*la_objects)[ "libcube4" ] =
            la_object( "libcube4",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-4.4-TP3/build-frontend",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-install/lib",
                       libs,
                       ldflags,
                       rpaths,
                       dependency_las );
    }

    libs.clear();
    ldflags.clear();
    rpaths.clear();
    dependency_las.clear();
    libs.push_back( "-lz" );
    if ( la_objects->find( "libcube4" ) == la_objects->end() )
    {
        (*la_objects)[ "libcube4" ] =
            la_object( "libcube4",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-4.4-TP3/build-frontend",
                       "/home/kassuskley/Desktop/khedma/internship/julich/assignment/report/cubelib-install/lib",
                       libs,
                       ldflags,
                       rpaths,
                       dependency_las );
    }

